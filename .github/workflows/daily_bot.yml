name: Daily Psychology Quiz Bot

# 언제 실행할지 설정
on:
  schedule:
    # 한국 시간 아침 8시 (UTC 23:00)마다 실행
    - cron: '0 23 * * *'
  workflow_dispatch: # GitHub 웹사이트에서 버튼 눌러서 수동 실행 가능

permissions:
  contents: write

jobs:
  update-quiz:
    runs-on: ubuntu-latest
    steps:
      # 1. 저장소 코드를 가져옴
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. 파이썬 설치
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. 필요한 라이브러리 설치
      - name: Install dependencies
        run: |
          pip install requests feedparser

      # ★ 중요: Ollama는 GitHub 서버에 없으므로 설치해줘야 함
      - name: Install & Start Ollama
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
          ollama serve &
          sleep 5
          ollama pull gemma2

      # 4. 파이썬 스크립트 실행 (파일 생성)
      - name: Run Generator
        run: |
          cd factory_brain
          python generator.py

      # 5. 생성된 파일을 GitHub에 업로드 (Git Push)
      - name: Commit and Push changes
        run: |
          git config --global user.name "AutoBot"
          git config --global user.email "bot@noreply.github.com"
          git add master_quiz_app/assets/*
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 봇이 새로운 심리테스트를 배달했습니다!" && git push)
